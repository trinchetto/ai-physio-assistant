# Core dependencies
pydantic>=2.0.0

# YAML parsing for exercise content
pyyaml>=6.0

# Image generation (SDXL)
# Note: These require significant disk space and GPU memory
# Install separately with: pip install -r requirements-image-gen.txt
# diffusers>=0.25.0
# transformers>=4.36.0
# accelerate>=0.25.0
# torch>=2.0.0

# Future dependencies (uncomment when needed)
# google-cloud-firestore>=2.0.0
# google-cloud-storage>=2.0.0
# google-cloud-aiplatform>=1.0.0
# streamlit>=1.30.0
# weasyprint>=60.0
