# Image Generation Dependencies
#
# These packages require significant disk space (~10GB) and GPU memory.
# Recommended: NVIDIA GPU with 8GB+ VRAM, or Apple Silicon Mac.
#
# Installation:
#   pip install -r requirements-image-gen.txt
#
# For CUDA (NVIDIA GPU):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# For Mac (Apple Silicon):
#   pip install torch torchvision (MPS backend is automatic)
#
# For CPU-only (slow, not recommended):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# Hugging Face Diffusers - SDXL support
diffusers>=0.25.0

# Transformers for text encoders
transformers>=4.36.0

# Accelerate for efficient model loading
accelerate>=0.25.0

# PyTorch (install appropriate version for your hardware first)
torch>=2.0.0

# Image processing
Pillow>=10.0.0

# Safetensors for efficient model loading
safetensors>=0.4.0

# Optional: xformers for memory-efficient attention (NVIDIA only)
# xformers>=0.0.23

# Optional: bitsandbytes for quantization (reduces VRAM usage)
# bitsandbytes>=0.41.0
